<%= form_for @entity ,url:group_entities_path(params[:group_id]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :amount%>
  <%= f.number_field :amount%>
  <%= f.submit %>
<% end %>